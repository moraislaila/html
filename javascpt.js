const valor = [3, 7, 18, 21, 33]

    console.log(valor)

    function inserirFinalArray(){
        var novovalor = prompt("digite seu valor")
        valor.push(novovalor)
        console.log(valor)
    }

        function calcularmedia(){
            var soma = 0 , media = 0 
            for(i=0; i < numeros.length; i++){
             soma += numeros[i];
            }
            media = soma / numeros.length
            alert(media)
            }


            
